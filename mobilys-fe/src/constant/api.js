/**
 * API constants.
 *
 * Purpose: store base URLs, endpoint paths, timeouts, and other API-related config
 * so values are not hardcoded across components/services.
 */

export const API = {
  auth: {
    backendDetail: {
      noProjectAssigned: "You are not assigned to any project yet.",
    },
  },
};
